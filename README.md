# Jenkins Task-2 (GitHub Webhook + Auto Build + Email)

## What this does
When I push a commit to GitHub, Jenkins automatically triggers a build and emails the build output.

## Files
- build.sh : simple script executed by Jenkins
- screenshots/ : proof screenshots (job config, webhook, build console, email)

## Flow
GitHub Commit -> GitHub Webhook -> Jenkins Build -> Email Notification
